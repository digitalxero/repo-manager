language: python
python:
    - 3.4
install:
    - pip install -e . --use-mirrors
before_script:
    - pip install -r test_requirements.txt --use-mirrors
script:
    - py.test repo_manager --strict --cov=repo_manager --cov-report=term-missing --pep8
after_success:
  - coveralls
